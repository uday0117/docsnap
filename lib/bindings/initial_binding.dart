import 'package:get/get.dart';

class InitialBinding extends Bindings {
  @override
  void dependencies() {
    // All dependencies are pre-registered in main() before runApp.
    // Nothing to do here.
  }
}
